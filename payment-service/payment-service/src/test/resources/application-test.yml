spring:
    security:
        oauth2:
            client:
                registration:
                    payment-service:
                        provider: payment
                        client-id: test-service-client
                        client-secret: test-secret
                        authorization-grant-type: client_credentials
                        scope: api.internal, account:read
                provider:
                    payment:
                        token-uri: test-uri
            resourceserver:
                jwt:
                    issuer-uri: http://test-issuer
                    jwk-set-uri: http://test-issuer/jwks

    h2:
        console:
            enabled: true
    datasource:
        hikari:
            driver-class-name: org.h2.Driver
            jdbc-url: jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
            username: sa
            password:

    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        properties:
            hibernate:
                format:
                    sql: true
            highlight:
                sql: true
            hbm2ddl:
                auto: create
            dialect: org.hibernate.dialect.PostgreSQLDialect
        open-in-view: false
        show-sql: true

iamport:
  api-secret: dGVzdC1hcGktc2VjcmV0
  base-url: https://api.portone.io
  mid-key: test-mid-key
  webhook-secret: dGVzdC13ZWJob29rLXNlY3JldA==

logging:
  level:
    org:
        hibernate:
            orm:
                jdbc:
                    bind: info
        spring:
            transaction:
                interceptor: info
